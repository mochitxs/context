# Copia este archivo como ".env" y rellena tus propios valores.
# El archivo ".env" real NUNCA se sube a GitHub (ya está en .gitignore).

# Entorno de ejecución: "local" o "production"
APP_ENV=local

# Conexión a la base de datos
DB_NAME=context_db
DB_USER=root
DB_PASS=
